#include "Command.h"
#include <stdio.h>

void doCommand(Command * command, SignalsList * signals, DynamicArray *undoStack, DynamicArray *redoStack)
{
	/*switch (command->commandValue)
	{
	default:
		printf("Invalid command value!");
		break;
	case 1:
		addSignal(command->signal.id, command->signal.modulatedSignal, command->signal.type, command->signal.priorityNumber, signals);
	case 2:
		updateSignal(command->signal.id, command->signal.modulatedSignal, command->signal.type, command->signal.priorityNumber, signals);
	case 3:
		deleteSignal(command->signal.id, signals);
	}*/
}
